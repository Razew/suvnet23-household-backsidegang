import { Text, View } from 'react-native';
import { container, large } from '../themes/styles';

export default function RegisterScreen() {
  return (
    <View style={container}>
      <Text style={large}>Register screen</Text>
    </View>
  );
}
